#Here is the procedure to fetch the bootstrap files from the devel branch:

wget -O esg-bootstrap http://distrib-coffee.ipsl.jussieu.fr/pub/esgf/dist/devel/esgf-installer/2.1/esg-bootstrap --no-check-certificate
chmod 555 esg-bootstrap
./esg-bootstrap --devel
